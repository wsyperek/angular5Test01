import { FlightCityPipe } from './flight-city.pipe';

describe('FlightCityPipe', () => {
  it('create an instance', () => {
    const pipe = new FlightCityPipe();
    expect(pipe).toBeTruthy();
  });
});
